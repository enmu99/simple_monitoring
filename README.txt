#README for Pepi's monitors
#
#
#critical_handler.sh:
#Executes the script but only sends push notification in case the value is critical.
#
#mon_handler.sh:
#Executes the script and sends a push either if it's OK, or NOT OK.
#
#Crontab Configuration:
#Configure crontab to execute critical_handler.sh once every 10 seconds.
#Configure crontab to execute mon_handler.sh once each hour.
#
#
#
#
